services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: aip-dma-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN:?set TUNNEL_TOKEN in .env.cloudflare}
    extra_hosts:
      - "host.docker.internal:host-gateway"
